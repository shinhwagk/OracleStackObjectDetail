{
    "name": "table object",
    "input": {
        "type": "select",
        "value": "select 'a', 'b' from dual"
    },
    "details": [
        {
            "name": "abc",
            "query": "select * from abc where a=? and b=?"
        },
        {
            "name": "33",
            "query": "select * from ccc where a=? and b=?"
        }
    ]
}