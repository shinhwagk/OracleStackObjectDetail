{
  "name": "table object",
  "input": {
    "type": "option",
    "value": "select 'a', 'b' from dual"
  },
  "objects": [
    {
      "query": "select * from abc where a=? and b=?" // or query file because sql text long
    }, {
      "query": "select * from ccc where a=? and b=?"
    }
  ]
}
